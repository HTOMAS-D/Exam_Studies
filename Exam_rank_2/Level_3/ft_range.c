int *ft_range(int start, int end)
{
	int i = 0;
	int temp = start;
	int fill = 0;
	while (temp <= end)
	{
		i++;
		temp++;
	}
	int *arr = (int *) malloc(sizeof(int) * (i + 1);
	arr[i] = NULL
	while (i >= 0)
	{
		arr[end] = end;
		if (start > end)
			end++;
		else
			end--;
	}
	return (arr);
}